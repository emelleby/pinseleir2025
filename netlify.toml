[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "vite"
  port = 5173

# Redirect all routes to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
